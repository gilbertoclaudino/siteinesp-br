"use client";
import { FormEvent,useState } from "react";
export function ContactForm(){const [state,setState]=useState("idle"),[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const f=e.currentTarget,d=new FormData(f),p=Object.fromEntries(d.entries());p.consentimento=d.get("consentimento")==="on";try{const res=await fetch("/api/contato",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(p)}),r=await res.json() as {protocolo?:string;error?:string};if(!res.ok)throw new Error(r.error||"Falha no envio");setState("success");setMsg(`Solicitação registrada. Protocolo ${r.protocolo}.`);f.reset()}catch(x){setState("error");setMsg(x instanceof Error?x.message:"Não foi possível enviar.")}}return <form className="contact-card" onSubmit={submit}><div className="form-grid"><label>Nome completo<input name="nome" maxLength={120} required/></label><label>E-mail<input name="email" type="email" maxLength={160} required/></label><label>Telefone ou WhatsApp<input name="telefone" maxLength={30}/></label><label>Cidade/UF<input name="cidade" maxLength={100}/></label></div><label>Assunto<select name="assunto" required defaultValue=""><option value="" disabled>Selecione</option><option>Mega Vestibular Social</option><option>Vestibular da Mulher</option><option>Parceria institucional</option><option>Voluntariado</option><option>Transparência</option><option>Outro assunto</option></select></label><label>Mensagem<textarea name="mensagem" rows={6} maxLength={2000} required/></label><label className="honeypot">Website<input name="website" tabIndex={-1}/></label><label className="check"><input name="consentimento" type="checkbox" required/><span>Autorizo o tratamento destes dados para resposta à solicitação, conforme a Política de Privacidade.</span></label><button className="pill dark" disabled={state==="sending"}>{state==="sending"?"Enviando...":"Enviar solicitação"} <b>↗</b></button>{msg&&<p className={`form-status ${state}`} role="status">{msg}</p>}</form>}
